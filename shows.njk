---
layout: main
---

{% import 'components.njk' as components %}

<style>
  .showList {
    list-style-type: none;
    margin: 0;
    padding: 0;
  }

  .show {
    margin-bottom: 2em;
    border: 1px solid #ccc;
    padding: 12px;
    border-radius: 12px;
  }

  .show_row {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 0.5em;
    margin-top: 0.25em;
  }

  .show_tag {
    padding: 0.5em;
    font-size: 0.75em;
    background: var(--link-color);
    color: var(--background-dark);
    border-radius: 0.5em;
  }

  .show_tag:last-child {
    border-right: none;
  }
</style>

<section class="shows">
  <h2>Upcoming dates:</h2>

  {% if shows.upcoming.length == 0 %}
    <p>There are no shows scheduled at the moment.</p>
  {% else %}
    <ol class="showList">
      {% for show in shows.upcoming %}
        <li class="show">
          <div class="show_row">
            <div class="show_tag">{{ show.category }}</div>
            <div class="show_tag">{{ show.artistName }}</div>
          </div>

          <div class="show_row">
            <div>
              {% if show.link %}
                <a href="{{ show.link }}">{{ show.title }}</a>
              {% else %}
                {{ show.title }}
              {% endif %}
            </div>
          </div>
          <div class="show_row">{{ show.humanDate }}</div>
          {% if show.venue %}
            <div class="show_row">{{ show.venue }}</div>
          {% endif %}
          <div class="show_row">{{ show.location }}</div>
        </li>
      {% endfor %}
    </ol>
  {% endif %}

  <h2>Past dates:</h2>

  <ol class="showList">
    {% for show in shows.past.reverse() %}
      <li class="show">
        <div class="show_row">
          <div class="show_tag">{{ show.category }}</div>
          <div class="show_tag">{{ show.artistName }}</div>
        </div>

        <div class="show_row">
          <div class="show_title">
            {% if show.link %}
              <a href="{{ show.link }}">{{ show.title }}</a>
            {% else %}
              {{ show.title }}
            {% endif %}
          </div>
        </div>
        <div class="show_row">
          <div>{{ show.humanDate }}</div>
        </div>
        {% if show.venue %}
          <div class="show_row">{{ show.venue }}</div>
        {% endif %}
        <div class="show_row">
          <div>{{ show.location }}</div>
        </div>
      </li>
    {% endfor %}
  </ol>

  <p>
    I did my best to remember every show I've ever played. If you remember one
    that's missing, let me know!
  </p>
</section>

<section>
  <h2>Coldbrew Collective</h2>
  <p>
    Coldbrew Collective was a visual duo consisting of Aashish Gadani and Bobby
    Azarbayejani.
  </p>
  <p>
    If you're looking for shows I did visuals for as part of Coldbrew
    Collective, you can find a full list on
    <a href="https://aashishgadani.com/cc_show_list.html"
      >Aashish Gadani's website</a
    >.
  </p>
</section>
